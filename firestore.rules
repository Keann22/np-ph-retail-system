rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily allow all authenticated users read/write access for debugging.
      // This is not a secure rule for production but is necessary to isolate the permission issue.
      allow read, write: if request.auth != null;
    }
  }
}

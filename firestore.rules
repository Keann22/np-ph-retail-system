rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read and write access to all data.
      // This is a temporary and insecure rule for debugging purposes.
      allow read, write: if true;
    }
  }
}
